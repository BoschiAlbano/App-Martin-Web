BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Articulo] ADD [Oferta] BIT NOT NULL CONSTRAINT [Articulo_Oferta_df] DEFAULT 0;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
